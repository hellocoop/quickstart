export const clientId = 'hello_quick_start';
export const redirectUri = window.location.origin + '/';
export const wallet = window.location.origin.replace('quickstart', 'wallet');
export const admin = window.location.origin.replace('quickstart', 'admin');
